/**
 * Jellycat "New" page URL to monitor for new product arrivals.
 * When new products appear at the top of this page, all subscribers get alerted.
 */
export const JELLYCAT_NEW_URL = 'https://jellycat.com/new';

/**
 * Maximum number of products to track from the /new page.
 * We only care about the top products - these are the newest arrivals.
 */
export const MAX_TRACKED_PRODUCTS = 20;
